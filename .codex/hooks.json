{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "GRAPHIFY_BIN=$(command -v graphify || true); if [ -z \"$GRAPHIFY_BIN\" ] && [ -x \"$HOME/.local/bin/graphify\" ]; then GRAPHIFY_BIN=\"$HOME/.local/bin/graphify\"; fi; if [ -n \"$GRAPHIFY_BIN\" ]; then \"$GRAPHIFY_BIN\" hook-check; fi"
          }
        ]
      }
    ]
  }
}
